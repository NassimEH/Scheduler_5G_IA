apiVersion: v1
kind: Pod
metadata:
  name: test-pod-2
  labels:
    pod_type: UPF
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests:
        cpu: 100m
        memory: 128Mi


